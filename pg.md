# Placement Groups

List pg status:
```bash
ceph pg stat
```

Dump all PG details
```bash
ceph pg dump
```

Get the details of a pg:
```bash
ceph pg 1.0 query
```

List by pool:
```bash
ceph pg ls-by-pool rbd_pool
```
